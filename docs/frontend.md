# Airbnb India Clone — shared UI

The Next.js TypeScript interface is split between reusable code in the repository-level `components/` and `lib/` directories and Next App Router entry points in `app/`. All seeded locations are in India and all prices are formatted in INR.
